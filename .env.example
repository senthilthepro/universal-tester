# Universal Tester - Environment Configuration Example
# Copy this file to .env and fill in your actual values

# ============================================================================
# LLM PROVIDER CONFIGURATION
# ============================================================================
# Choose your LLM provider: "azure_openai", "google", or "ollama"
# 
# Ollama (FREE - Recommended for local development):
#   - Run any open-source LLM locally (Llama, Mistral, CodeLlama, etc.)
#   - No API costs
#   - 100% privacy - your code never leaves your machine
#   - Perfect for enterprises with strict security policies

# =============================================================================
# Azure OpenAI Configuration
# =============================================================================
# AZURE_OPENAI_API_KEY=your_azure_openai_api_key
# AZURE_OPENAI_DEPLOYMENT=gpt-4
# AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# =============================================================================
# Google Gemini Configuration
# =============================================================================
# GOOGLE_API_KEY=your_google_api_key
# GOOGLE_MODEL=gemini-2.0-flash:generateContent

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# LLM Behavior
OPENAI_IS_STREAMING=false
OPENAI_TEMPERATURE=0.3
OPENAI_REQUEST_TIMEOUT=60
OPENAI_MAX_RETRIES=2

# Application Settings
CHAINLIT_PORT=8000

# Debug Configuration
# DEBUG_MODE: When set to true, forces logging level to DEBUG regardless of LOG_LEVEL setting
# - true: Enable all debug logging (overrides LOG_LEVEL)
# - false: Use LOG_LEVEL setting (default)
DEBUG_MODE=false

# Logging Level Configuration
# Available LOG_LEVEL options:
# - DEBUG: Shows all messages including detailed debug info
# - INFO: Shows normal operation messages (default)
# - WARNING: Shows only warnings and errors
# - ERROR: Shows only errors
LOG_LEVEL=INFO

# Test Generation Configuration
# Maximum number of LLM auto-fix iterations for validation issues (default: 3, range: 1-10)
MAX_ITERATIONS=3

# Test Generation Settings
# EXCLUDE_PRIVATE_METHOD_TESTS: Control whether to generate test cases for private methods
# - false (default): Generate tests for private methods using reflection
# - true: Skip private method testing entirely, focusing only on public/protected methods
# This can reduce test complexity and improve maintainability when private method testing is not desired
EXCLUDE_PRIVATE_METHOD_TESTS=false

# INCREMENTAL_TEST_GENERATION: Control incremental test case file generation
# - false (default): Always create new test files (overwrite existing)
# - true: Check for existing test files and create additional numbered files (e.g., Test2.java, Test3.java)
#         with only uncovered test methods that are not present in existing test files
# This feature helps when you already have some test cases and want to add only missing coverage
INCREMENTAL_TEST_GENERATION=true

# DEVELOPMENT_FOCUS_FILE: Focus on specific Java file for development testing
# - Empty or None (default): Process all files in the project (normal mode)
# - Specific filename: Only process the specified file for faster development testing
# Examples: "StringHelper.java", "UserService.java", "ProductController.java"
# This is useful during development to test changes on a specific class without processing the entire project
DEVELOPMENT_FOCUS_FILE=none

# =============================================================================
# CUSTOM PROMPTS (OPTIONAL)
# =============================================================================

# Custom System Prompt - Override the default system instructions for the AI
# Leave empty to use the built-in system prompt
# Example: "You are an expert Java testing specialist focused on enterprise applications..."
CUSTOM_SYSTEM_PROMPT=

# Custom User Prompt Addition - Additional instructions added to every test generation request  
# This will be appended to the standard prompts for extra customization
# Examples:
# "Focus on security testing and edge cases. Include performance considerations."
# "Generate tests with extensive JavaDoc comments and detailed assertions."
# "Prioritize integration testing patterns and database transaction testing."
CUSTOM_USER_PROMPT_ADDITION=

# Prompt Enhancement Options
USE_CUSTOM_SYSTEM_PROMPT=false
USE_CUSTOM_USER_PROMPT_ADDITION=false

# =============================================================================
# EXAMPLE CUSTOM PROMPTS (Copy and modify as needed)
# =============================================================================

# Example 1: Security-focused testing
# CUSTOM_SYSTEM_PROMPT=You are a security-focused Java testing expert. Generate comprehensive test suites that prioritize security vulnerabilities, input validation, and secure coding practices.
# CUSTOM_USER_PROMPT_ADDITION=Focus heavily on security testing: SQL injection prevention, XSS protection, input sanitization, authentication/authorization edge cases, and secure data handling.

# Example 2: Performance-oriented testing  
# CUSTOM_SYSTEM_PROMPT=You are a performance-focused Java testing specialist. Create test suites that include performance benchmarks, memory usage validation, and scalability considerations.
# CUSTOM_USER_PROMPT_ADDITION=Include performance tests with timing assertions, memory leak detection, and concurrent execution scenarios. Add JMH benchmarks where appropriate.

# Example 3: Enterprise integration focus
# CUSTOM_SYSTEM_PROMPT=You are an enterprise Java testing expert specializing in Spring Boot, microservices, and distributed systems testing.
# CUSTOM_USER_PROMPT_ADDITION=Generate tests with @MockBean, @TestContainers, database transactions, message queues, and inter-service communication patterns.
DEBUG_MODE=false

# Application environment: development, staging, production
APP_ENV=development

# ============================================================================
# Optional Overrides
# ============================================================================

# Override application name (optional)
# APP_NAME_OVERRIDE=My Custom Tester

# Override application description (optional)
# APP_DESCRIPTION_OVERRIDE=My custom test generator

# Override author name (optional)
# AUTHOR_OVERRIDE=Your Name
